import { connect } from "react-redux";

const MisProyectos = () => {
    return (
        <div>
            Mis proyectos
        </div>
    );
};

const mapStateTopProps = state => ({

});

export default connect(mapStateTopProps, {

})(MisProyectos);

